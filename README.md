# Hubsync

Live: https://hubsync-g0tf.onrender.com/

## Setup

1. Install dependencies:
   - `npm install`
2. Create a `.env` file from `.env.example`.
3. Run locally:
   - `npm run dev`

## Deploy

Build with:
- `npm run build`



